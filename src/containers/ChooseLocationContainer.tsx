import React from 'react';
import styled from 'styled-components';

const ChooseLocationContainer: React.FC = () => <Wrapper>지역선택 컨테이너</Wrapper>;

const Wrapper = styled.div``;

export default ChooseLocationContainer;
