import React from 'react';
import styled from 'styled-components';

const ChooseDateContainer: React.FC = () => <Wrapper>날짜 선택 컨테이너</Wrapper>;

const Wrapper = styled.div``;

export default ChooseDateContainer;
